[pytest]
testpaths = tests
asyncio_mode = auto
# Don't print warnings about missing .env in test runs — the CI workflow
# injects env vars directly, so the .env file won't exist.
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
